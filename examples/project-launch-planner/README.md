# Project Launch Planner

This credential-free advanced example pairs a read-only MCP tool with a static MCP Apps UI. It uses fixed mock data, makes no network requests, and requires no environment variables. It intentionally omits `.app.json`; link a real registered ChatGPT app ID with `chatgpt-spawn link-app` only after registration.
