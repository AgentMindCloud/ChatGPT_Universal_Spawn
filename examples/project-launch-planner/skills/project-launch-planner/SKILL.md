---
name: project-launch-planner
description: Use when demonstrating or testing the credential-free project launch MCP example, including milestones, owners, risks, and the status interface.
---

# Project Launch Planner

Use only the mock data returned by the `get_launch_plan` tool. Call `get_launch_plan` once, summarize blocked items, and offer the interactive resource when the client supports MCP Apps UI. Clearly label all results as demonstration data.
